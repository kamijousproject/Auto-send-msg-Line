# 🎨 คู่มือใช้งาน GUI

คู่มือการใช้งาน LINE LDPlayer Automation ผ่าน GUI

---

## 🚀 เปิดใช้งาน

### วิธีที่ 1: Quick Start (ง่ายที่สุด)
```bash
quick_start.bat
# เลือก: 1
```

### วิธีที่ 2: รันตรงๆ
```bash
python gui.py
```

---

## 📋 หน้าต่าง GUI

### 🎯 Header
- **Title:** LINE LDPlayer Automation 🤖
- **Status:** แสดงสถานะการเชื่อมต่อ
  - ⚪ ไม่ได้เชื่อมต่อ
  - 🟢 เชื่อมต่อแล้ว

### 📑 Tabs (5 tabs)

---

## 1️⃣ Tab: Dashboard 📊

### ส่วน: การเชื่อมต่อ 🔌

**ปุ่ม:**
- **🔌 เชื่อมต่อ LDPlayer**
  - คลิกเพื่อเชื่อมต่อกับ LDPlayer
  - จะ initialize ldconsole + ADB + LINE automation
  - ต้องทำก่อนส่งข้อความ

- **🔌 ตัดการเชื่อมต่อ**
  - คลิกเพื่อตัดการเชื่อมต่อ
  - ปิด ADB connection

### ส่วน: การทำงานด่วน ⚡

**ปุ่ม:**
- **📤 ส่งข้อความทันที**
  - ส่งข้อความหาเพื่อน N คนแรก
  - ใช้ข้อความที่เปิดใช้งานอันแรก
  - จำนวนเพื่อนตามที่ตั้งไว้

- **🔍 ตรวจสอบความพร้อม**
  - รัน check_setup.py
  - ตรวจสอบว่าทุกอย่างพร้อมหรือไม่

- **🧪 ทดสอบ LDConsole**
  - เปิด demo_ldconsole.py
  - ทดสอบการควบคุม emulator

### ส่วน: สถิติ 📊

แสดงข้อมูล:
- จำนวนข้อความทั้งหมด / เปิดใช้งาน
- จำนวนตารางเวลาทั้งหมด / เปิดใช้งาน
- สถานะการเชื่อมต่อ
- สถานะ Scheduler

---

## 2️⃣ Tab: ข้อความ 💬

### ส่วน: ข้อความทั้งหมด 📝

**Listbox:**
- แสดงรายการข้อความทั้งหมด
- ✅ = เปิดใช้งาน
- ⭕ = ปิดใช้งาน
- **คลิก** เพื่อเลือกและแก้ไข

### ส่วน: รายละเอียด ✏️

**ฟิลด์:**
- **ชื่อ:** ชื่อข้อความ (สั้นๆ)
- **ข้อความ:** เนื้อหาข้อความ (รองรับหลายบรรทัด)
- **เปิดใช้งาน:** ☑️ = ใช้งาน, ☐ = ไม่ใช้

**ปุ่ม:**
- **➕ เพิ่มใหม่:** สร้างข้อความใหม่
- **💾 บันทึก:** บันทึกการแก้ไข
- **🗑️ ลบ:** ลบข้อความที่เลือก

### 📝 วิธีใช้:

#### เพิ่มข้อความใหม่:
1. คลิก **➕ เพิ่มใหม่**
2. ระบบจะสร้างข้อความใหม่
3. แก้ไข **ชื่อ** และ **ข้อความ**
4. คลิก **💾 บันทึก**

#### แก้ไขข้อความ:
1. **คลิก** ข้อความในรายการ
2. แก้ไขใน **รายละเอียด**
3. คลิก **💾 บันทึก**

#### ลบข้อความ:
1. **คลิก** ข้อความที่จะลบ
2. คลิก **🗑️ ลบ**
3. ยืนยัน

---

## 3️⃣ Tab: ตั้งเวลา ⏰

### ส่วน: ตารางเวลา 📅

**Listbox:**
- แสดงตารางเวลาทั้งหมด
- Format: `[Status] [Time] - [Description]`
- ✅ = เปิดใช้งาน
- ⭕ = ปิดใช้งาน

### ส่วน: รายละเอียด ✏️

**ฟิลด์:**
- **เวลา (HH:MM):** เวลาที่จะส่ง (เช่น `09:00`, `14:30`)
- **ข้อความ:** เลือกข้อความที่จะส่ง (dropdown)
- **วัน:** เลือกวันที่จะส่ง (☑️ = ส่ง, ☐ = ไม่ส่ง)
  - จันทร์, อังคาร, พุธ, พฤหัส, ศุกร์, เสาร์, อาทิตย์
- **เปิดใช้งาน:** ☑️ = ใช้งาน, ☐ = ไม่ใช้

**ปุ่ม (จัดการ):**
- **➕ เพิ่มใหม่:** สร้างตารางใหม่
- **💾 บันทึก:** บันทึกการแก้ไข
- **🗑️ ลบ:** ลบตารางที่เลือก

**ปุ่ม (ควบคุม):**
- **▶️ เริ่ม Scheduler:** เริ่มระบบส่งอัตโนมัติ
- **⏹️ หยุด Scheduler:** หยุดระบบ

### 📝 วิธีใช้:

#### เพิ่มตารางใหม่:
1. คลิก **➕ เพิ่มใหม่**
2. ตั้งค่า:
   - **เวลา:** เช่น `09:00`
   - **ข้อความ:** เลือกจาก dropdown
   - **วัน:** เลือกวันที่ต้องการ
   - **เปิดใช้งาน:** ☑️
3. คลิก **💾 บันทึก**

#### เริ่ม Scheduler:
1. ตรวจสอบว่า **เชื่อมต่อ LDPlayer แล้ว** (ใน Dashboard)
2. คลิก **▶️ เริ่ม Scheduler**
3. โปรแกรมจะส่งอัตโนมัติตามเวลา
4. คลิก **⏹️ หยุด** เมื่อต้องการหยุด

#### ตัวอย่างการตั้งเวลา:
- **ส่งทุกวันธรรมดา เวลา 09:00:**
  - เวลา: `09:00`
  - วัน: ☑️ จันทร์-ศุกร์
  
- **ส่งวันเสาร์-อาทิตย์ เวลา 14:00:**
  - เวลา: `14:00`
  - วัน: ☑️ เสาร์, อาทิตย์

---

## 4️⃣ Tab: ตั้งค่า ⚙️

### ส่วน: LDPlayer 🎮

**ฟิลด์:**
- **ldconsole.exe:** Path ไปยัง ldconsole.exe
  - ปุ่ม 📁: เลือกไฟล์
  - Default: `C:\LDPlayer\LDPlayer9\ldconsole.exe`

- **adb.exe:** Path ไปยัง adb.exe
  - ปุ่ม 📁: เลือกไฟล์
  - Default: `C:\LDPlayer\LDPlayer9\adb.exe`

- **Emulator:** ชื่อ emulator
  - พิมพ์ชื่อ emulator (ดูได้จาก LDPlayer)
  - Default: `LDPlayer` หรือ `0`

### ส่วน: Automation 🤖

**ฟิลด์:**
- **จำนวนเพื่อน:** จำนวนเพื่อนที่จะส่ง (1-10)
  - Default: 3

- **หน่วงเวลา (วินาที):** หน่วงเวลาระหว่างส่ง
  - Default: 2
  - แนะนำ: อย่างน้อย 2 วินาที

**ปุ่ม:**
- **💾 บันทึกการตั้งค่า:** บันทึกลง config.json
- **🔄 โหลดค่าเดิม:** โหลดจาก config.json

### 📝 วิธีใช้:

#### ตั้งค่าครั้งแรก:
1. คลิก 📁 เพื่อเลือก **ldconsole.exe**
2. คลิก 📁 เพื่อเลือก **adb.exe**
3. ใส่ **ชื่อ emulator**
4. ปรับ **จำนวนเพื่อน** และ **หน่วงเวลา**
5. คลิก **💾 บันทึกการตั้งค่า**

---

## 5️⃣ Tab: Logs 📝

### Log Viewer

แสดง logs แบบ real-time:
- สีดำพื้นหลัง (dark theme)
- Font: Courier (monospace)
- Auto-scroll ล่างสุด

**ปุ่ม:**
- **🔄 รีเฟรช:** โหลด logs จากไฟล์
- **🗑️ ล้าง:** ล้าง logs ที่แสดง

### 📝 Log Format:
```
[HH:MM:SS] ข้อความ
```

**ตัวอย่าง:**
```
[10:30:15] 🔌 กำลังเชื่อมต่อ LDPlayer...
[10:30:18] ✅ เชื่อมต่อสำเร็จ!
[10:30:20] 📤 กำลังส่งข้อความ...
[10:30:35] ✅ ส่งสำเร็จ 3/3
```

---

## 💡 Tips การใช้งาน

### 1. ลำดับการใช้งานครั้งแรก:
```
1. Tab: ตั้งค่า → ตั้งค่า paths
2. Tab: Dashboard → คลิก "เชื่อมต่อ LDPlayer"
3. Tab: ข้อความ → เพิ่ม/แก้ไขข้อความ
4. Tab: Dashboard → คลิก "ส่งข้อความทันที" (ทดสอบ)
5. Tab: ตั้งเวลา → เพิ่มตารางเวลา
6. Tab: ตั้งเวลา → คลิก "เริ่ม Scheduler"
```

### 2. ทดสอบก่อนใช้จริง:
- ตั้ง **จำนวนเพื่อน = 1**
- ส่งให้ตัวเอง (Saved Messages)
- ตรวจสอบว่าส่งถูกต้อง
- ค่อยเพิ่มจำนวน

### 3. ดู Logs เสมอ:
- Tab: Logs
- จะแสดงทุกอย่างที่โปรแกรมทำ
- ใช้สำหรับ debug

### 4. บันทึกบ่อยๆ:
- หลังแก้ไขข้อความ → คลิก **💾 บันทึก**
- หลังแก้ไขตารางเวลา → คลิก **💾 บันทึก**
- หลังแก้ไขการตั้งค่า → คลิก **💾 บันทึกการตั้งค่า**

### 5. Scheduler:
- ต้องเชื่อมต่อก่อน
- เปิดโปรแกรมค้างไว้
- ปิดโปรแกรม = หยุด Scheduler

---

## ⚠️ ข้อควรระวัง

### ❌ อย่าทำ:
- ❌ ส่งข้อความบ่อยเกินไป
- ❌ ปิดโปรแกรมขณะกำลังส่ง
- ❌ เปลี่ยนหน้าจอ emulator ขณะกำลังส่ง

### ✅ ควรทำ:
- ✅ ทดสอบก่อนใช้จริง
- ✅ เช็ค logs บ่อยๆ
- ✅ บันทึกการตั้งค่า
- ✅ ปิด Scheduler ก่อนปิดโปรแกรม

---

## 🐛 แก้ปัญหา

### ปัญหา: กดปุ่มแล้วไม่มีอะไรเกิดขึ้น

**แก้:**
1. ดู Tab: Logs
2. อ่าน error message
3. แก้ตามที่แจ้ง

### ปัญหา: เชื่อมต่อ LDPlayer ไม่ได้

**แก้:**
1. Tab: ตั้งค่า
2. ตรวจสอบ paths (ldconsole.exe, adb.exe)
3. ตรวจสอบชื่อ emulator
4. ตรวจสอบว่า LDPlayer เปิดอยู่

### ปัญหา: ส่งข้อความไม่ถูกที่

**แก้:**
1. หน้าจอ emulator อาจต่างจาก default
2. ต้องแก้ไข `modules/line_automation.py`
3. ปรับตำแหน่งใน `get_friend_positions()`

### ปัญหา: Scheduler ไม่ทำงาน

**แก้:**
1. ตรวจสอบว่า **เชื่อมต่อแล้ว**
2. ตรวจสอบว่า **เปิดใช้งาน** ตารางเวลา
3. ตรวจสอบ **เวลาที่ตั้ง** ถูกต้อง (HH:MM)
4. ตรวจสอบ **วัน** ที่เลือก

---

## 🎊 สรุป

GUI ช่วยให้:
- ✅ ใช้งานง่ายกว่า Command Line
- ✅ ไม่ต้องแก้ไข JSON files
- ✅ เห็น UI ชัดเจน
- ✅ จัดการข้อความและตารางเวลาได้ง่าย
- ✅ ดู logs แบบ real-time

**เหมาะสำหรับ:** ผู้ใช้ทั่วไป, ไม่ชอบ Command Line

**ไม่เหมาะสำหรับ:** Automation scripts, Headless servers

---

**พร้อมใช้งานแล้ว!** 🚀

```bash
python gui.py
```
